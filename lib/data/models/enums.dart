enum AccountType {
  cash,
  bank,
  credit,
  crypto,
}

enum TransactionType {
  expense,
  income,
  transfer,
}

enum RecurrenceType {
  daily,
  weekly,
  monthly,
  yearly,
}

enum CategoryType {
  expense,
  income,
}

enum BudgetType {
  monthly,
  timeBound,
}

enum LoanType {
  personal,
  mortgage,
  auto,
  student,
  business,
  other,
}

enum TransactionStatus {
  pending,
  posted,
  scheduled,
}
